[project]
name = "super-agent"
version = "0.1.0"
description = "基于 LangChain deepagents + OpenSandbox + PostgreSQL 的深度智能体（多轮对话 / 长期记忆 / 沙箱执行）"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.7.13",
    "opensandbox>=0.1.15",
    "langgraph-checkpoint-postgres>=3.1.2",
    "langchain-openai>=1.6",
    "psycopg[binary,pool]>=3.2",
    "python-dotenv>=1.0",
]
