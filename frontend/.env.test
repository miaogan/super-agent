# 测试环境：前端构建时注入
# 后端 API 地址（独立部署时填后端实际可访问地址；同源走 nginx 反代时留空用 /api 相对路径）
VITE_API_URL=http://localhost:8000
# 前端自身访问地址（用于 OAuth 回调等场景）
VITE_FRONTEND_URL=http://localhost:8080
# 环境标识（前端展示用）
VITE_ENV=test
